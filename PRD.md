# typort -- 产品需求文档 (PRD)

**项目名称**: typort (Typst + export)
**版本**: v0.1
**日期**: 2026-05-16
**状态**: 规划阶段

---

## 1. 产品愿景与定位

### 1.1 问题陈述

中文社科学术界存在一个普遍的工作流断裂：研究者用 Typst 撰写论文以获得优质排版体验，但期刊投稿系统普遍只接受 Word (.docx) 格式。目前没有任何工具能将 Typst 文档高保真、结构化地转换为 Word 文件。

现有方案的核心缺陷：

- **typ2docx**: 依赖 PDF 中间路径（PDF -> Word），语义结构全部丢失，标题变成带格式的文本，脚注变成手动编号，数学公式变成图片
- **Pandoc**: Typst reader 仅做文本级解析，不执行完整编译，丢失 show rule、import、计数器等关键特性
- **手工排版**: 研究者被迫在 Typst 定稿后手动将内容复制到 Word 模板中重新排版，耗时且易出错

### 1.2 产品定位

**typort** 是一个纯 Rust 命令行工具，通过直接映射 Typst 内容树到 OOXML XML，将 Typst 文档转换为结构化的 Word 文件。它是第一个以中文社科论文为一等公民的 Typst-to-Word 转换器。

**核心理念**：语义保留 (Semantic Preservation) -- 标题仍是标题，脚注仍是脚注，公式仍是可编辑公式，而非扁平化的带格式文本。

### 1.3 一句话描述

> 填补中文社科期刊 Typst -> Word 转换的空白，让研究者用 Typst 写作、用 Word 投稿，无需手动重排。

---

## 2. 目标用户

### 2.1 主要用户画像

| 属性 | 描述 |
|------|------|
| 身份 | 中文社科领域研究者（教授、博士生、硕士生、独立研究者） |
| 学科 | 法学、经济学、社会学、政治学、管理学、教育学等社科学科 |
| 写作工具 | 日常使用 Typst 撰写论文，享受其简洁语法和优质排版 |
| 投稿需求 | 目标期刊要求提交 .docx 格式，且对版式有严格规定（字体、字号、行距、页边距、章节编号） |
| 技术水平 | 能使用命令行工具；熟悉 Typst 但不一定熟悉 OOXML 或 Rust |
| 痛点 | 每次投稿都要花数小时将 Typst 稿件手动搬到 Word 模板中 |

### 2.2 次要用户

- 学术期刊编辑部：可能将 typort 纳入稿件处理流程
- 高校学位论文办公室：支持学生使用 Typst 撰写、以 Word 提交学位论文
- 学术会议论文集编辑：统一将 Typst 来稿转为 Word 以便汇编

---

## 3. 核心功能与优先级

### P0 -- 基础转换能力（MVP 必须）

| 功能 | 说明 |
|------|------|
| **文本与段落** | Typst 段落映射为 `w:p`/`w:r`，保留粗体、斜体、下划线、删除线等行内格式 |
| **CJK 字体** | 正确设置 `w:rFonts` 的 `eastAsia` 属性；支持宋体、仿宋、楷体、黑体与 Times New Roman 的中英文混排 |
| **标题与中文章节编号** | `HeadingElem` 映射为 Word Heading 样式，通过 `numbering.xml` 实现“一、”“(一)”“1.”“(1)”“①”五级编号 |
| **脚注与页内重编号** | `FootnoteElem` 映射为 `w:footnoteReference`；通过 `w:numRestart val="eachPage"` 实现逐页重编号，使用 ① ② ③ 编号样式 |
| **页面设置** | 页面大小、页边距、行距映射到 `w:sectPr`、`w:pgMar`、`w:spacing` |
| **基础样式** | 生成 `styles.xml`，定义正文、标题、脚注文本、摘要、关键词等命名样式 |
| **数学公式** | Typst 数学内容树（`EquationElem` 及其子元素）直接映射为 OMML（`m:oMath`/`m:oMathPara`），支持分式、根号、上下标、矩阵、定界符、累加/积分等常用结构 |

### P1 -- 完整论文结构

| 功能 | 说明 |
|------|------|
| **参考文献** | 从 Typst 编译后的内容树中提取已渲染的引用标注和文献列表，保留格式输出到 Word |
| **交叉引用** | `RefElem` 映射为 Word 交叉引用或文本引用（如“见图 1”“见表 2”） |
| **图片与图注** | `FigureElem` 中的图片嵌入 `word/media/`，图注映射为带编号的 Caption 样式段落 |
| **表格** | `TableElem` 映射为 `w:tbl`，支持单元格合并、边框、对齐 |
| **列表** | `ListElem`/`EnumElem` 映射为 Word 编号/项目符号列表 |
| **期刊预设** | 以 TOML/YAML 配置文件定义期刊版式要求（字体族、字号、行距、页边距、编号方案），一条命令即可切换目标期刊 |

### P2 -- 高级功能

| 功能 | 说明 |
|------|------|
| **Word 原生文献域代码** | 生成 Word 可识别的 `CITATION` 域代码，便于后续在 Word 中管理引文 |
| **目录生成** | `OutlineElem` 映射为 Word TOC 域（`w:sdt` + `w:fldChar`） |
| **页眉页脚** | 期刊名、卷期号、作者姓名等写入 `header1.xml`/`footer1.xml` |
| **首页脚注元数据** | 基金项目、作者简介、收稿日期、中图分类号、文献标识码等常见首页脚注字段 |
| **代码块** | `RawElem` 映射为等宽字体段落或表格框 |

---

## 4. 非目标

以下明确不在 typort 的范围内：

- **通用格式转换器**: typort 不是“万能转换器”，只做 Typst -> Word 一个方向，且聚焦学术论文场景
- **模板系统**: typort 不提供 Word 模板编辑 GUI；期刊预设通过 TOML 配置实现，不做可视化模板设计
- **LaTeX 支持**: 不接受 LaTeX 输入，不做 LaTeX -> Word 转换
- **PDF 中间路径**: 不走 PDF -> Word 的间接路线；整条管线基于语义内容树
- **双向转换**: 不做 Word -> Typst 的反向转换
- **排版引擎**: typort 不重新实现 Typst 的排版逻辑，它依赖 Typst 编译器获取内容树
- **完美像素级还原**: 目标是语义结构等价，不是视觉像素级一致；Word 的渲染引擎与 Typst 不同，细微排版差异不可避免

---

## 5. 技术方案概要

详细技术分析见[可行性研究报告](../typst-to-docx-feasibility-study.md)。此处仅列出关键技术决策。

### 5.1 架构

```
Typst 源文件 (.typ)
        |
        v
 typst crate (v0.14.x) 编译
        |
        v
 Content 内容树（语义结构完整）
        |
        v
 自定义 Realization Pass（参照 HTML 导出模型）
        |
        v
 元素分发器（按 HeadingElem / ParElem / FootnoteElem / EquationElem ... 分类处理）
        |
        +------+--------+--------+--------+
        |      |        |        |        |
     文本   标题/脚注  数学    表格/图   文献
    转换器   转换器   转换器   转换器   转换器
        |      |        |        |        |
        +------+--------+--------+--------+
                        |
                        v
               OOXML 构建层
        (docx-rs + quick-xml 原生 XML 补充)
                        |
                        v
                 .docx ZIP 输出
```

### 5.2 关键技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| Typst 编译 | `typst` crate v0.14.x | 官方编译器，提供完整内容树 |
| 内容树遍历 | 自建，参照 `typst-html` 的 Converter 模式 | HTML 导出已验证此路径可行 |
| OOXML 基础生成 | `docx-rs` (bokuweb) v0.4.20 | 支持段落/run/样式/脚注/编号/页面设置/CJK 字体，覆盖 80% 需求 |
| OOXML 扩展生成 | `quick-xml` 原生 XML | 补充 OMML 数学、逐页脚注重编号、高级节属性等 docx-rs 不覆盖的部分 |
| OMML 数学序列化 | `ooxml-omml` v0.1.0 + `quick-xml` | 直接从 Typst 数学内容树映射到 OMML XML |
| CSL 参考文献 | 从编译后内容树提取已渲染文本（MVP）；后续可引入 `hayagriva` | 避免重复实现引文解析逻辑 |
| 期刊预设 | TOML 配置文件 + `toml` crate | 声明式、易维护、社区可贡献 |

### 5.3 与 Typst 内部 API 的关系

typort 深度依赖 Typst 编译器内部 crate（`typst`、`typst-syntax`、`typst-library`）。这些 API 明确标记为不稳定，每次 Typst 版本更新可能引入破坏性变更。

缓解策略：
- 锁定特定 Typst 版本
- 在 Typst 访问层引入适配器 trait，隔离下游代码
- 预留版本升级维护工时

---

## 6. 成功标准

### 6.1 MVP 成功标准（Phase 1-2 完成时）

- [ ] 一篇典型中文社科论文（含标题、摘要、关键词、正文、脚注、参考文献）经 typort 转换后，在 Word 中打开结构完整
- [ ] 标题保持 Heading 1-5 样式层级，可被 Word 导航窗格识别
- [ ] 脚注为 Word 原生脚注（非手动编号文本），支持逐页重编号
- [ ] CJK 字体正确渲染（宋体正文、黑体标题，英文 Times New Roman）
- [ ] 页边距、行距符合至少一个目标期刊的版式要求

### 6.2 完整版成功标准（Phase 3-5 完成时）

- [ ] 数学公式在 Word 中为可编辑的 OMML 公式（非图片）
- [ ] 通过 3 个以上真实期刊模板的验收测试
- [ ] 从 Typst 源文件到 .docx 输出的端到端转换时间 < 10 秒（普通论文）
- [ ] 在 Word 2019、Word 365、WPS Office 中均可正常打开并渲染

### 6.3 用户体验成功标准

- [ ] 单条命令完成转换：`typort input.typ -o output.docx`
- [ ] 指定期刊预设：`typort input.typ -o output.docx --preset 管理世界`
- [ ] 转换后无需手动调整即可直接提交（至少对已配置预设的期刊）

---

## 7. 竞品对比

| 维度 | **typort** | **typ2docx** | **Pandoc (Typst reader)** |
|------|-----------|-------------|--------------------------|
| **转换路径** | 内容树 -> OOXML（直接语义映射） | PDF -> Word + 数学源码提取 -> Pandoc 合并 | Typst 源码文本解析 -> Pandoc AST -> docx |
| **语义保留** | 完整（标题/脚注/引用保持 Word 原生结构） | 丢失（所有结构扁平化为定位文本） | 部分（不执行 show rule、import、计数器） |
| **数学公式** | 可编辑 OMML（直接映射） | 通过 Pandoc texmath 转换，保真度有限 | 通过 texmath 转换，保真度有限 |
| **中文支持** | 一等公民（CJK 字体、中文编号、逐页脚注重编号） | 无专门优化 | 无专门优化 |
| **技术栈** | 纯 Rust，单二进制 | Python + Rust + XSLT + JS（6 种语言） | Haskell（需安装 Pandoc） |
| **依赖** | 无外部依赖 | 依赖 Pandoc + Adobe/pdf2docx | 依赖 Pandoc |
| **期刊预设** | 内建 TOML 配置系统 | 无 | 无（需自制 reference.docx） |
| **脚注** | Word 原生脚注 + 逐页重编号 | 丢失脚注结构 | 基础脚注（无逐页重编号） |
| **成熟度** | 开发中 | 已发布（PyPI） | 已发布（Pandoc 内建） |
| **许可证** | MIT/Apache-2.0（计划） | 待确认 | GPL-2.0 |

---

## 8. 约束与假设

### 约束

- 依赖 Typst 编译器内部 API（pre-1.0，不稳定）
- OMML 数学无现有 Rust 转换库，需从零构建映射逻辑
- 不同版本 Word 对 OOXML 的渲染存在差异
- 期刊版式要求各异，需逐个适配预设

### 假设

- 用户已安装 Rust 工具链或使用预编译二进制
- 用户的 Typst 文档遵循合理的结构化写法（使用 heading、footnote 等语义元素，而非手动格式化）
- 目标期刊接受 Word 2016+ 兼容的 .docx 文件
- Typst 的 HTML 导出架构（content-tree-based realization）在后续版本中保持基本稳定

---

## 9. 开放问题

1. **Typst API 稳定性**: 是否需要同时维护多个 Typst 版本的适配层？还是只跟踪最新稳定版？
2. **数学公式降级策略**: 当遇到 OMML 无法表达的 Typst 数学结构时，是否回退为图片嵌入？
3. **WPS Office 兼容性**: WPS 对 OMML 的支持程度是否足够？是否需要提供 MathType 兼容模式？
4. **期刊预设众包机制**: 如何组织社区贡献期刊预设？是否需要独立的预设仓库？
5. **自定义 show rule**: 用户在 Typst 中通过 show rule 改变元素外观，这些改变能否/是否应该反映在 Word 输出中？
