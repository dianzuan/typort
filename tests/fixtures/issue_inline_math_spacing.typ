// Test: spaces around inline math equations preserved
// typ2docx: spaces between inline equations and regular text are missing
// This is critical for readability

Let $x$ be a variable and $y$ be another.

The formula $E = m c^2$ is famous.

We know that $a + b = c$ implies $a = c - b$ in general.
