// Test: equation ending with punctuation
// typst-hs #50: number followed by dot causes parse error
// Pain point: trailing period in display equation is very common

Thus we obtain
$ 0 -> A -> B -> C -> 0. $

Also valid:
$ a + b, $
