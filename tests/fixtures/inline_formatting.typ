= Inline Formatting Test

This has #super[superscript] and #sub[subscript] text.

This has #underline[underlined] and #strike[strikethrough] text.

This has #highlight[highlighted] and #smallcaps[Small Caps] text.

This has `inline code` and #raw("raw text") in it.
