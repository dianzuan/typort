[workspace]
members = [
    "crates/typort-cli",
    "crates/typort-core",
    "crates/typort-ooxml",
    "crates/typort-math",
    "crates/typort-presets",
]
resolver = "2"

[workspace.package]
version = "0.1.1"
edition = "2024"
license = "Apache-2.0"
description = "Universal Typst to Word (.docx) converter"
repository = "https://github.com/dianzuan/typort"
homepage = "https://github.com/dianzuan/typort"

[workspace.dependencies]
# Typst ecosystem
typst = "0.14.2"
typst-library = "0.14.2"
typst-syntax = "0.14.2"
typst-kit = { version = "0.14.2", default-features = false, features = ["fonts", "embed-fonts", "packages", "vendor-openssl"] }
typst-utils = "0.14.2"
typst-realize = "0.14.2"
typst-html = "0.14.2"
typst-pdf = "0.14.2"

# SVG rasterization
resvg = { version = "0.45", default-features = false }
tiny-skia = "0.11"

# OOXML generation
quick-xml = "0.37"
zip = { version = "2", default-features = false, features = ["deflate"] }

# Serialization
serde = { version = "1", features = ["derive"] }
toml = "0.8"

# CLI
clap = { version = "4", features = ["derive"] }

# Internal crates
typort-core = { version = "0.1.0", path = "crates/typort-core" }
typort-ooxml = { version = "0.1.0", path = "crates/typort-ooxml" }
typort-math = { version = "0.1.0", path = "crates/typort-math" }
typort-presets = { version = "0.1.0", path = "crates/typort-presets" }

# Bibliography
hayagriva = "0.9.1"
